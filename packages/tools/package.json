{
  "name": "tools",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./lib",
    "compile": "tsc -p tsconfig.json",
    "build": "npm run clean && npm run compile",
    "tool-1": "npm run build && node ./lib/tool-1",
    "tool-2": "npm run build && node ./lib/tool-2"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.9.5"
  }
}
